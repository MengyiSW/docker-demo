events {}

http {
    server {
        listen 80;
        server_name localhost;

        access_log off;
        error_log /dev/null crit;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}